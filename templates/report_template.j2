<!DOCTYPE html>
<html>
<head>
    <title>Ansible Report Genrated by Ansble.</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #333; }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 8px;
            text-align: left;
            border: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Ansible Report</h1>
    <p>Date: {{ ansible_date_time.date }}</p>
    <h2>Host Summary</h2>
    <table>
        <tr>
            <th>Hostname</th>
            <th>Status</th>
            <th>Message</th>
        </tr>
        {% for host, result in results.items() %}
        <tr>
            <td>{{ host }}</td>
            <td>{{ result.status }}</td>
            <td>{{ result.msg }}</td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>